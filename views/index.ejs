<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TodoList Application</title>
</head>
<body>
  <h1>My TodoList</h1>
  <p>Don't be weak, try to complete what you've intend to!</p>

  <!-- My Input to add new todos -->
  <form action="/add" method="post">
    <label for="todo">Enter you todo:</label>
    <input type="text" name="todo" id="todo" />
    <button type="submit">
      Add  
    </button>
  </form>

  <ul>
    <% for(var i = 0; i < todos.length; i++) { %>
      <li>
        <%= todos[i].task %>
        <!-- Delete Action -->
        <form action="/delete" method="post" style="display: inline;">
          <input type="hidden" name="todo" value="<%= todos[i].id %>" />
          <button type="submit">Delete</button>
        </form>
      </li>
    <% } %>
  </ul>

</body>
</html>

<!-- CRUD - CREATE, READ, UPDATE, DELETE -->

<!-- READ -->
<!-- CREATE -->
<!-- DELETE -->